# Access code for the password gate (client-side, hashed before comparison)
NEXT_PUBLIC_ACCESS_CODE=

# Google Maps JavaScript API key for the Interactive Site Plan overlay.
# Required scopes: Maps JavaScript API + (optional) Map ID for AdvancedMarker.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
